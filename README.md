# Cosine

Built by **Dedeepya Avancha** ([@Dd1235](https://github.com/Dd1235)) · [Dd1235/Cosine](https://github.com/Dd1235/Cosine)

Search 3,400+ competitive-programming problems the way you actually think about them — by keyword, by describing the idea, or by technique.

**Live at [onebysec.com](https://onebysec.com/)** · [live stats](https://onebysec.com/stats.html) — first load after idle takes ~a minute (free tier waking up); it's fast after that.

(The walkthrough maybe stale, please do check out the live deployment)

![Cosine walkthrough in dark mode](demo/deliverables/walkthrough-dark.gif#gh-dark-mode-only)
![Cosine walkthrough in light mode](demo/deliverables/walkthrough-light.gif#gh-light-mode-only)

## What I'd like feedback on

Four open problems. Each one I've already measured, and each is stuck on a judgement call rather than on code — so an opinion is worth more here than a patch.

**1. Difficulty doesn't cross judges. Should it?**
Filtering and sorting by difficulty work _inside one judge_ and are refused across two, because I can't defend a shared scale. Codeforces ratings are calibrated so a problem rated R is roughly a coin flip for a contestant rated R; AtCoder's community estimates use the same definition but a different population; LeetCode has three buckets and no rating. I tried acceptance rate as the bridge and measured it: it separates LeetCode's tiers on the full problemset (AUC 0.677) but **inverts on this corpus** (0.426), because most Mediums here were selected _for_ low acceptance while every Hard came in unfiltered. So the honest options look like (a) anchor on people who compete on several judges and fit a mapping from their ratings, (b) infer difficulty from solve counts and accept the population bias, or (c) stay per-judge forever and make that legible in the UI. **Which would you actually trust a recommendation from?**

**2. CSES publishes no difficulty at all — 400 problems, 12.5% of the corpus.**
They're invisible to every difficulty filter and every sort, which is bad because they're some of the best problems here. The CSES book is _roughly_ ordered within each section, so a section-relative signal ("hard for a Range Queries problem") is derivable. **Is that useful, or is a relative-only number more misleading than none?** The alternative is inferring difficulty from done-marks once there's enough traffic, which needs users I don't have yet.

**3. Are the "my level" heuristics right?** Details in [Tracking the grind](#how-my-level-picks-a-band) below. Specifically:

- **Codeforces / AtCoder:** the band is `[your rating, +200]`. Is that the right window, or should it start below you (`R-100`) so there's warm-up in it? Standard practice advice says solve _above_ your rating; the band takes that literally.
- **AtCoder:** I compare your AtCoder rating against AtCoder problem difficulties, which is scale-consistent — but is it _true_ in practice, or does the AtCoder rating distribution sit differently against its own difficulty estimates than Codeforces does?
- **LeetCode:** there's no rating, so it reads solved counts and picks a tier plus an acceptance-rate half. The rungs are at 10 / 25 / 100 Hards. Those numbers are a guess. Worse, LeetCode's counts cover the whole problemset, most of which is far easier than anything here — so someone with 300 Mediums solved may have solved 300 easy Mediums. **Is there a better signal LeetCode actually exposes?**

**4. Umbrella terms, and the one label I still can't fill.**
Searching the technique directory for "range queries" used to return nothing — the vocabulary is all leaves (`segment-tree`, `sparse-table`, `mo-algorithm`) with no node above them. There are 12 umbrella groups now — `combinatorics` was the most recent, added because the model labels the specific trick (`stars-and-bars`, `inclusion-exclusion`) and nobody searches for that until they already know the answer; **which ones are still missing?** Separately: `sparse-table` sits on exactly 1 problem (CSES's _Static Range Minimum Queries_) and `prim` on none. Three LLM audit passes over ~180 candidates — static-RMQ problems, LCA problems, every segment-tree problem — produced zero more, and neither judge tags "sparse table" so I can't go fetch them either. The likely truth is that both are _implementations_ rather than problem types: a sparse table is a subroutine inside an LCA solution, and MST problems get solved with Kruskal. **If you know a problem here where a sparse table is genuinely the point, that's the most useful bug report I could get.**

Open an issue, or mail me or send me a text on wa or linkedin — see [Feedback → fix](#feedback--fix) for what previous rounds changed.

## Why this exists

Google "cses euler path" and you get the CSES homepage, a blog post, and Wikipedia — not the actual Euler-path problems. "knapsack leetcode" is no better at surfacing the good knapsack DPs. Site tags help but stop at their site's border and at coarse labels: LeetCode has `dynamic-programming`, but nothing for `digit-dp`, `wqs-binary-search`, or `slope-trick`. And nothing anywhere answers _"I remember the story — a thief robbing houses — what was that problem?"_

Cosine is one search box over LeetCode, CSES, Codeforces and AtCoder that handles all three.

## What that looks like

Real output from the live corpus, same query run both ways. **keyword** is BM25 over titles, statements and technique labels; **meaning** is cosine over sentence embeddings. Neither wins everywhere, which is why you can switch.

| query                  | keyword (bm25)                                                                                         | meaning (dense)                                                                                               |                                                                                                                                                        |
| ---------------------- | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `cat and mouse`        | Cat and Mouse · Cat and Mouse II · Mice and Cheese — **4 hits, all of them the ones you meant**        | Cat and Mouse II · Cat and Mouse · Mice and Cheese · Design a Text Editor · Food Buckets to Feed the Hamsters | exact words → exact answers; semantic drifts into "small animal" territory                                                                             |
| `cheese`               | Mice and Cheese. That's it — **one hit**                                                               | Giant Pizza · Hamburgers · Mice and Cheese · Pizza With 3n Slices · Banquet Preparations                      | no problem is _about_ cheese, so keyword has nothing to match. Meaning finds the food cluster anyway                                                   |
| `thief robbing houses` | House Robber · House Robber II · Count the Number of Houses at a Certain Distance II                   | House Robber · House Robber II · **Diamond Theft** · Profitable Schemes                                       | you remember the story, not the title. Keyword rides the word "house" into unrelated house problems; meaning finds the heist that never says "rob"     |
| `aliens trick`         | Best Time to Buy and Sell Stock IV · Minimum Partition Score · Min Total Space Wasted With K Resizings | Design Tutorial: Increase the Constraints · Qpwoeirut and Vertices · Planets Queries I                        | **the reversal.** "aliens trick" expands to `wqs-binary-search` and keyword nails it; the embedding model has never heard the phrase and returns noise |
| `string dp graph`      | **Longest Palindromic Path in Graph** — tagged `graph` + `dynamic-programming` + `string`, all three   | Interleaving String · Strange Printer                                                                         | stack technique words and the top hit is the problem that carries all of them                                                                          |
| `mex set`              | Sliding Window Mex · MEX Queries · Maximize Mex                                                        | MEX Queries · Mex Grid Construction · Sliding Window Mex                                                      | niche vocabulary. No judge tags `mex`, so this search doesn't exist anywhere else                                                                      |
| `sweep line`           | Intersection Points · Perfect Rectangle · Area of Rectangles                                           | Lines and Queries II · Line Segments Trace I · Erect the Fence                                                | the word "sweep" appears in **zero** problem statements — only the labels carry it                                                                     |

The pattern: **keyword wins when you know the vocabulary** — a technique name, a title fragment, a community nickname. **Meaning wins when you only remember what the problem was about.** Typing a problem's exact name always puts that problem first, whichever mode you're in.

## Finding problems

- **A word the corpus has never seen is still a question — for the meaning ranker.** `rat` matches nothing literally, and semantic search answers it with *Cat and Mouse · Save More Mice · Mice and Cheese*; the status line says *nothing uses "rat" — nearest by meaning*, because that is a weaker claim than a match. Keyword mode still says the word appears nowhere, which is the honest answer when BM25 genuinely scores zero. The honest limit: a name is shaped like a word, so `deepya` now gets labelled neighbours in meaning mode rather than the clean zero it used to get.
- **Two search modes, one box.** **keyword** matches words in the title, statement _and_ our technique labels — a problem that opens "Alice and Bob play a game…" never says "dp", but its label does, so `game theory dp` still finds it. **meaning** takes plain english: `thief robbing houses` finds House Robber. Pick one next to the search button; the status line says which answered, and how fast. (A third mode, `hybrid`, was retired: fusing the two inherited semantic search's inability to return zero results, so nonsense queries came back confident.)
- **Technique labels deeper than "dp".** Every problem carries curated pattern labels from a 183-label taxonomy (plus 182 community aliases) — `digit-dp`, `wqs-binary-search`, `slope-trick`, `xor-basis`, `dsu-on-tree`. Every label has at least one problem behind it — a label that answers a search with silence got retired into an alias instead. Click any label to filter results to it; browse the whole taxonomy with per-label counts on the patterns page.
- **Umbrella terms, because the taxonomy is all leaves.** Looking up "range queries" used to return nothing: the vocabulary has `segment-tree`, `sparse-table`, `sqrt-decomposition` and `mo-algorithm`, but nothing said what they had in common. Groups now sit above the labels — and only fill silence, so typing "tree" still lists every label containing "tree" rather than being swapped for a curated family. The same pass fixed a plainer bug: the filter compared prose to slugs, so "segment tree" never matched `segment-tree`.
- **A page for what exists.** You can't search for a technique you've never heard of, and most people's exposure stops at one sheet. The patterns page lists all 182 labels with counts and a filter: type `dp` and get the whole family — `digit-dp`, `tree-dp`, `slope-trick`, `state-compression`. Umbrella terms work too — the taxonomy is all leaves, so "range queries" matches no label's name, but it resolves to the 12-label family (`segment-tree`, `sparse-table`, `mo-algorithm`, …) instead of silence. Each chip clicks through to the problems carrying it. It's a directory, not a tutorial, and it stays out of the search page.
- **Plurals are the same question.** `graph` and `graphs` return the same problems, and so do `vertices`/`vertex` and `queries`/`query` — a plural folds into its singular, but only when that singular is a word this corpus contains, so `series` and `bfs` and `kruskals` are left alone. Before this, `graphs` returned 18 problems to `graph`'s 546, and they were the wrong 18.
- **Misspellings still land.** `djikstra` and `kruskals` used to return literally nothing; now a word the corpus has never seen gets its nearest real term appended — and only such words, so a query that already worked can't change. What it will *not* do is guess at short words: the first letter has to match, a three-letter term can only be completed (`tre` → `tree`) not respelled, and a four-letter target needs more than a handful of problems behind it. Keyboard mash returns nothing in either mode — `asdkjhqwe` and `qqqqq` are refused on shape, since no cosine cutoff separates them from real words (`xkcdqq` scores 0.565 against `rat`'s 0.296).
- **Community names just work.** Type "aliens trick" and the query quietly expands to `wqs-binary-search` — the status line shows `+wqs binary search`, so you also learn the canonical name.
- **Difficulty, on each judge's own terms.** Select a judge and its native scale appears: Easy/Medium/Hard for LeetCode, a from/to rating range for Codeforces and AtCoder — set both ends the same and you get exactly 1500-rated problems, which fixed bands couldn't express. There is deliberately no shared scale — a CF 1600 is not "the same as" a LeetCode Medium and pretending otherwise would be a lie — so a band filters only its own judge and leaves the others alone. CSES publishes no difficulty, so it simply offers none. Pick a LeetCode tier and an **acceptance-rate range** appears under it, because three buckets are too coarse for 661 Mediums — `hard` + `10-30%` is the hardest Hards. It's offered only _after_ a tier is chosen, and that restriction is the interesting part: across tiers the number lies in this corpus. The same rule governs **sorting**: pick one judge and you can order easiest- or hardest-first; pick two and the option isn't offered, because nothing places a Medium against a 1600. While searching, sorting reorders the top 20/50/100 by relevance rather than re-picking from the whole match set — otherwise "sort by difficulty" silently discards the ranking you searched for and hands back the corpus's easiest problems.
- **Filters that stack.** Judges are a set, not a choice — toggle `lc` `cses` `cf` `atc` above the results, or click the tag on any card. They compose with technique labels, with done/not-done, and with your saved lists: `:bookmarks` + `cf`+`atc` + not-done is your unfinished Codeforces and AtCoder saves. Judges persist across searches; a technique label doesn't, because it's a drill-down into what you were reading and the median label covers a single problem. Shareable as `?platform=codeforces,atcoder`.
- **Find similar.** One click on any result lists its nearest neighbors by meaning (cosine over precomputed vectors, ~1 ms). Built for post-contest upsolving: open the problem that beat you, see its family.
- **Compare rankers.** `:compare thief robbing houses` runs the query on bm25 and dense side by side with rank deltas — watch keyword search find nothing for "cheese" while semantic nails it. A command rather than permanent chrome, since it's an engine-tuning tool.
- **Where a proxy metric stops being honest.** Acceptance rate looks like a free difficulty scale, and on the full LeetCode problemset it is one — median Medium 57.5%, Hard 47.3%, AUC 0.677. Measured on _my_ slice it inverts to 0.426: served Hards look easier than served Mediums. Not a data bug — 554 of 661 Mediums were selected _because_ they had the lowest acceptance rates, while every Hard came in unfiltered. So it ships as a within-tier filter and a within-tier sort tiebreak, never a scale. Inside one tier the selection bias is gone and the number is just the number.
- **A deliberately hard corpus.** 3,461 problems across four judges: every LeetCode Hard and the lowest-acceptance-rate Mediums (contest Q3/Q4 grade), all of CSES, and Codeforces sampled across rating bands from 800 up and AtCoder from its own scale. The 800–1000 band exists so a filter set to a beginner's rating has something to return; it was added and benchmarked, and moved nothing — bm25 flat on every metric, Recall@100 unchanged, 2.5% of top-5 slots displaced ([experiments/10](experiments/10-easy-band-and-level-filters.md)). No Easy filler — the best hard problems beat 4,000 padded ones. Fresh problems flow in through one refresh command, with niche labels human-reviewed before they ship.

## Tracking the grind

- **Filters that know your level.** With handles saved, a `my level` chip sets each selected judge's band from _that judge's own_ numbers — never a shared scale. Hover it to see the reasoning and how many problems it selects; press it again to drop it. Exactly how it decides is below.

*In practice:* press **connect sheet** in the library bar → grant access once → a spreadsheet called **cosine notes** appears in your Drive with a row per saved problem. After that you never press sync again unless something goes wrong: bookmark, tick done or write a note and the row is written a few seconds later. Write in either place — the `✎` editor here or the sheet itself. Your cosine login and your Google account are independent — sign in here with one address and hold the sheet in another if you like. Connect later from a different Google account and the site tells you plainly — that account can't see the sheet, which is sitting safely in the first one — instead of quietly starting a second sheet and stranding your notes. Notes are cached in your browser, so a refresh shows them without contacting Google, and a note you write while the sheet is unreachable waits in this browser until it can be delivered.

![The library, filtered to bookmarks, with a problem's own notes pulled from the sheet](demo/04-sheet-notes.png)

*Above: the revision filters (`marked: 1mo+ / 3mo+ / 6mo+`, oldest-first), the `✎` marker on an annotated problem, and a solution summary written in the sheet showing up on the card.*
- **One profile, every judge.** Save LeetCode / Codeforces / CodeChef / AtCoder / GitHub handles: solved counts and ratings per platform, plus one combined activity heatmap with `dsa` / `dev` / `overall` tabs — because grinding contests and shipping code are both progress.
- **Your handles are encrypted, and the claim stops where the truth does.** Linked usernames and the stats fetched with them are AES-256-GCM ciphertext; the key lives in the app environment, never in the database, so a database copy is unreadable on its own. No other user can see them — there's no public profile and no leaderboard. The judges themselves necessarily receive the username when your stats are fetched, and I won't claim "not even the admin can see it", because I run the server and that would be false. It isn't stored readably, deleting is immediate, and the code is here to check.
- **Bookmarks and done-marks** on every result, with a shell-style library (`:bookmarks`, `:done`, Tab cycles views). Done-marks feed the heatmap too. **A library view takes a query**: `:done graph` is the graph problems you've finished, `:bookmarks segment tree` the ones you saved — matching titles and technique labels, every word required. Inside the library, age chips (`1mo+ / 3mo+ / 6mo+`) and oldest-first turn `:done` into a revision queue — start from what you solved longest ago. A saved problem also grows a one-letter rating chip — tap to cycle `· → A again → H hard → M medium → E easy` — and the library filters on it, so "hard, and I haven't looked at it in three months" is two clicks. It shows up in the library only, never on an ordinary search result, and it saves as you tap rather than making you wait. Nothing is scheduled off it; it's a label, not an SRS. Un-saving a problem keeps the rating — the row outlives both flags, so saving it again months later brings your `again` back with it. There's also a `notes` filter (written up / no note), a `to write up` chip for the pair nobody thinks to combine — done, and never explained to yourself — and `pick one`, which opens a random problem from whatever the filters currently select.
- **Notes that never touch this server.** Connect a Google Sheet and your saved problems mirror into it. Write notes either place: `✎` on a saved card opens an editor with markdown that survives a spreadsheet cell (`**bold**`, `` `code` ``, fenced blocks, bullets), and the sheet itself is still a perfectly good editor. It starts with one column for you — `solution_summary` — and any column you add is read back and shown on the card under the name you gave it, free-form. The site reads your notes and shows them on the card (✎ marks annotated problems), and writes exactly one of your cells — `solution_summary`, and only when you typed it in the editor here. The contract is one sentence: **the app writes six columns — `problem_id`, `title`, `link`, `judge`, `difficulty`, `recall` — on your library's rows, and nothing else, ever** — no deleting, no blanking, no touching anything you added. There is no `done` or `bookmarked` column: the site owns that state and shows it, and a column reading "yes" is a second place to look for something you already know. Rows are never removed either — un-save a problem and its row stays exactly as you left it, yours to delete. It finds those columns by their names in row 1 rather than by position, so it can't write into one of yours by miscounting. Sync also keeps the layout in one shape — the site's columns first, the suggested ones next, yours after — by *moving* columns rather than rewriting the grid, so formulas and formatting travel with them and the only thing ever removed is a duplicate column with nothing in it. The OAuth token is requested by your browser and stays there (`drive.file` scope — the app can only see the sheet it created). You consent once, on a click, the first time you connect; after that the sheet keeps itself current — a few seconds after any bookmark, done-mark or rating — because each page load asks Google for a fresh token in the mode that may not display anything (`prompt: none`). If it can't be answered without a question it fails, nothing appears, and nothing syncs — the button is there for that case, and one failure stops this browser being asked again. The trade, stated plainly: sync happens while the site is open; offline sync would mean storing a Google refresh token server-side, a credential more sensitive than the notes this design refuses to store.

### How "my level" picks a band

Three heuristics, one per judge, and **no judge is ever used to infer another** — that's the cross-judge normalization I don't have (see [feedback](#what-id-like-feedback-on) #1). A wrong guess about someone's level is a page of problems they can't attempt, so each judge is read only on its own scale.

| judge      | signal it reads        | band it sets                                 |
| ---------- | ---------------------- | -------------------------------------------- |
| Codeforces | your rating            | `[R, R+200]`, snapped to the 100-grid        |
| AtCoder    | your rating            | `[R, R+200]`, snapped to the 200-grid        |
| LeetCode   | solved counts per tier | one tier + an acceptance-rate half within it |

**Why the rated judges are the easy case.** Both scales are calibrated the same way: a problem rated R is one a contestant rated R solves about half the time under contest conditions, and AtCoder's community difficulty estimates carry that same definition. So `[R, R+200]` means the same thing on both — winnable but not free — with no conversion between them. Both ends are clamped to what the corpus actually holds, so a 3500-rated user gets the top of the corpus instead of an empty window above it, and a beginner gets the bottom.

**LeetCode is the real heuristic**, because LeetCode publishes no rating. What it does publish is solved counts per tier, so a four-rung ladder on the Hard count picks a tier, then which half of that tier's acceptance-rate range to use — split at the corpus's own median rather than an invented cut-off. Lower acceptance means harder, so the "gentler half" is the higher-acceptance one.

| hards solved | tier   | half of the tier | reading                       |
| ------------ | ------ | ---------------- | ----------------------------- |
| under 10     | Medium | gentler          | still building Medium fluency |
| 10–24        | Medium | sharper          | comfortable with Mediums      |
| 25–99        | Hard   | gentler          | Hards are working             |
| 100+         | Hard   | sharper          | Hards are routine             |

Volume of Mediums promotes off the gentlest rung: 100+ Mediums with no Hards is not a beginner, even though the Hard count alone can't tell. **Treat this one as coarse** — LeetCode's counts cover the whole problemset, most of which is easier than anything in this corpus, so 300 Mediums solved may be 300 easy Mediums.

Two rules keep it safe rather than clever: every suggestion **carries the count it would produce and is dropped if that count is zero** (a button that silently empties the page is worse than no button), and the suggestion is a plain `difficulty=` token parsed by the same filter a human click produces — the tests round-trip it and assert the promised count is the real one. `GET /api/level` reads only the cached profile stats and never calls a judge, so the search page never waits on leetcode.com; if the numbers were never fetched, the chip just doesn't appear.

## Small things that make it nice to use

- **The URL is the state.** Query, judges, difficulty, sort, pattern, ranker and filter all live in the address bar, so refresh keeps your place, back leaves in one step, and copying the URL shares exactly what you're looking at (`/?q=knapsack&platform=codeforces,atcoder&ranker=dense`).
- **It's a terminal.** Monospace everything, a prompt-style path that follows you (`~/search "graph cycle"`, `~/done`, `~/profile`), a typewriter status line, light/dark themes. Type `:help` and a man page types itself out — an index plus six addressable sections, so `:help filters` answers one question instead of handing you 180 lines.
- **Fast on purpose.** Keyword search answers in ~0.1 ms and semantic in ~2 ms; profile revisits paint instantly from a local snapshot and refresh in the background.
- **The 404 page plays Valid Parentheses.** There wasn't one — a typo'd URL fell off the end of the router and got Express's `Cannot GET /prfoile.html`. Now it's a real 404 (status and all) that leads with a search box, and underneath sits **unbalanced**: openers drop onto a stack, you press the matching closer to pop, and it ends either in a `stack overflow` or an expression that stopped being valid — which it prints back to you with a caret under the character that broke it. The rules are pure functions, so [the game has tests](web/notfound.test.js).
- **It shows its work.** Latency and ranker on every result set, query expansions made visible, a debug page exposing per-term scoring math, and a public stats page with real usage and live latency percentiles — including cold-start counts, because the free-tier instance sleeps and honesty beats mystery.

## Feedback → fix

A running log of what real users hit, and what changed. Most entries are labeling
or wording, not ranking — that turned out to be the pattern.

| Feedback                                                               | Fix                                                                                                                                                                                                                                                                    |
| ---------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| "aliens trick" found nothing                                           | Query-side alias expansion — community names now expand to the canonical label                                                                                                                                                                                         |
| The blinking bar under the box looked like the input                   | Search box takes focus on load; the cursor only shows while the status line types                                                                                                                                                                                      |
| Four ranker names (tfidf/bm25/dense/hybrid) meant nothing              | Three modes: **keyword** / **meaning** / **both**, explained in `:help`                                                                                                                                                                                                |
| Stats looked stale on the new domain                                   | Per-origin HTTP cache — window cut to 30s and an "as of" stamp added                                                                                                                                                                                                   |
| "mcm dp", "mex" returned nothing                                       | Statements are summaries, so missing terms are missing _labels_; added the vocabulary + an LLM audit loop                                                                                                                                                              |
| "flows" returned nothing                                               | No stemmer — added flow aliases and folded 9 scattered flow labels into the canonical set                                                                                                                                                                              |
| Similarity scores were confusing                                       | Numeric scores are debug-only now; results keep the relative bar                                                                                                                                                                                                       |
| Didn't know bookmarks / done / filters existed                         | `:help` gained a SAVING section and a real explanation of "find similar"                                                                                                                                                                                               |
| A cleared filter came back on refresh                                  | Clearing the pill now drops `?pattern=` from the URL too                                                                                                                                                                                                               |
| "LeetCode-only is limiting"                                            | Codeforces and AtCoder added — rating-stratified from 1300 up, no easies                                                                                                                                                                                               |
| "sweep line finds nothing, and CF tags don't have that keyword"        | Correct — Codeforces has no sweepline tag at all. Fixed by auditing the corpus for the technique directly; 17 problems gained the label                                                                                                                                |
| "can I see only Codeforces problems?"                                  | Click the judge tag on any result; combine several, clear with the pill                                                                                                                                                                                                |
| The "searching" line was distracting while typing                      | It animated one character at a time while the answer was already back — now it only appears if a search actually takes over 400ms                                                                                                                                      |
| "where is filter by judge?"                                            | It shipped as clickable result tags only, which nobody could see — added a plain dropdown next to the search box                                                                                                                                                       |
| Judge filter was one-at-a-time, and saved lists ignored it entirely    | Judges are now a multi-select chip set, and `:bookmarks` / `:done` / `:all` respect judge + done filters                                                                                                                                                               |
| The compare checkbox was clutter for anyone not tuning the engine      | Moved to a `:compare <query>` command                                                                                                                                                                                                                                  |
| Refreshing threw away the search                                       | The address bar now mirrors query + judges + pattern + ranker + filter, so refresh and bookmarking keep the view                                                                                                                                                       |
| Clicking a pattern chip could show 0 results                           | A filter with no query now browses that label instead of searching within your last one — `line-sweep` shows all 40, not the 0 that matched "cycle"                                                                                                                    |
| Should a pattern filter survive a new query?                           | No — measured it: carrying a label into a new query dead-ends in 9 of 25 cases, a judge in 0 of 20. Labels drop when you type, judges stay                                                                                                                             |
| "no section on what patterns even exist — I only know Striver's sheet" | Searches now name the technique family above the results, and the patterns page is filterable and out of the dev-tools nav                                                                                                                                             |
| "pick topics + difficulty, get 3 random problems"                      | Not shipped. The difficulty half can't work — 16% of the corpus has no difficulty and a band leaves 1-3 candidates per topic. A shuffle was tried and reverted: one label at a time is too narrow to be worth the chrome                                               |
| "2-sum was 10-11th, not first"                                         | Worse than reported — rank 28. Typing a problem's exact name now puts that problem first; `two sum`, `2 sum` and `3 sum` all work                                                                                                                                      |
| "hybrid returns results for a random name"                             | Semantic search gives every problem some similarity, so it can never return zero. Hybrid retired; a query whose words appear nowhere in the corpus now says so                                                                                                         |
| "UI for phone wasn't as good as desktop"                               | The search bar overflowed by ~200px and was being hidden rather than fixed. Full mobile pass: form restacks, inputs no longer trigger iOS zoom, heatmap and stats table scroll, tap targets from ~16px to 34px                                                         |
| "AVL tree and niche techniques missing"                                | AVL is a textbook structure, not a contest technique — `avl tree` now routes to the ordered-set problems the corpus actually has                                                                                                                                       |
| "can I filter by difficulty?"                                          | Per judge, on its own scale — LeetCode tiers, Codeforces and AtCoder rating bands. A band never filters a judge you didn't pick. Codeforces also grew 250 problems in the 1000–1299 range so the easier band has content                                               |
| "sort/filter by acceptance rate within Hards and Mediums"              | Shipped, scoped to one tier. Backfilled `acceptance_rate` onto all 1,434 LeetCode problems — and because `corpusHash` covers only the indexed text, it needed no re-embed                                                                                              |
| "notes on problems, but not in your database — sync my own Google Sheet?" | Built with browser-only OAuth: the token never leaves the tab, the server's whole involvement is serving a public client id. Column ownership replaces conflict resolution — the app writes status columns, you write note columns, no cell has two writers |
| "filter done problems by age, for revision" | Age chips in the library — `1mo+ / 3mo+ / 6mo+` plus oldest-first, so revision starts from what you solved longest ago. Fixing it exposed a real bug: a problem both bookmarked and done showed its *bookmark* age in `:done` while the list sorted by *done* date |
| "can I sort by difficulty? load more gets tricky"                      | It does. A sorted search is one fixed window of the best matches, so paging is withdrawn rather than left to produce an incoherent page 2 — you pick the window instead (top 20/50/100). Browsing has no ranking to protect, so it sorts everything and pages normally |
| "add the advanced techniques that show up in high-rated problems" | Researched rather than guessed: 268 problems rated 2400+ were audited for named algorithms they weren't labelled with. Nine techniques already had problems but no name in the vocabulary — `string-hashing` on 26 of them — and 81 labels were missing outright. `xor-basis`/`linear-basis` were one technique under two names, split like dsu-on-tree. Every label now has at least one problem behind it |
| "range queries on the patterns page gives me nothing" | Two bugs, one search: the filter compared prose to slugs (`segment tree` couldn't match `segment-tree`), and the taxonomy was all leaves with no umbrella node. The filter normalizes now, and 11 umbrella groups map "range queries", "flows", "dp optimization" to their families |
| "deque dp shows problems with deque in the title, not the dp trick" | The label is `monotonic-queue-optimization`, which shares no word with the query — aliased, plus an audit pass that labelled more of the family. Same alias machinery as `aliens trick` |
| "square root decomposition returns square roots" | It was scored as `square` + `root` + `decomposition`, and the corpus writes the technique as `sqrt`. Nine technique names now resolve as one phrase rather than loose words — measured at +0.183 nDCG@10 on the technique slice with nothing else regressing |
| "dsu on trees and small to large merging give different results" | They were two labels on two disjoint sets of problems, and `dsu` was separately expanding to `union-find`. One label now, and both names return the same four problems on both rankers |
| "z algorithm ranks 'given 3 integers x, y and z'" | The bare token `z` carries a higher idf than `decomposition`. `z algorithm` now resolves to the `z-function` label instead of the letter |
| "make it resistant to spelling mistakes" | `djikstra` and `kruskals` returned nothing at all; they now find the right problems. Only words the corpus has never seen are corrected, so a query that already worked cannot change |
| "on a phone the ranker picker pushes search down" | The option read `meaning · dense`; at the 16px iOS demands, that wrapped the form onto four rows. It reads `meaning` under 720px and keeps the full name on desktop |
| "bookmark and done sit somewhere different on every card" | They were anchored to the end of the meta text, whose width varies with the judge tag and whether there is a difficulty chip. Anchored to the card's right edge instead |
| "let me mark a solve again/hard/med/easy, and keep a revision section" | Half of it already existed and nobody could find it, which was the real finding. Shipped the rating as one cycling chip on saved problems only, filterable in the library — but not a fourth list: `:done` + `again` + `marked 3mo+` is the revision queue, out of filters that already compose. Rating writes its own column and never touches `done_at`, so re-rating an old solve can't reset the age filter it feeds |
| "too lazy to add problems to the sheet as I solve them" | You never add a row: bookmark or tick done on the site and sync writes the row — id, title, link, judge, difficulty, bookmarked, done, done date, rating. The sheet is where you write; the site fills in what it already knows |
| "`:help` is a wall of text" | 183 lines in one scroll, with judges and difficulty documented twice. Now an index plus six addressable sections — `:help filters`, `:help sheet` — each wrapped to 64 columns so it stops scrolling sideways on a phone. A test asserts every section is listed in the index, because a section nobody can reach is the same bug |
| First cut of the rating: "the chip algo doesn't look right… not easy to click… don't bloat normal search" | Three separate mistakes, all mine. The chip wrote the word, so it was a different width in every row and shoved the action group around — now one letter, sized to match ☆ and ✓. It waited on the server behind a disabled button, so four taps to reach "easy" meant four spinners — now it paints first and rolls back only if the save fails. And it appeared on every saved problem anywhere, including search results — now the library only. The filter went from six chips to one dropdown, which is also what a phone renders as a native picker |
| "what if I've marked something as *again*, but unmark it as done?" | It silently deleted the rating, because `CHECK (done OR bookmarked)` deletes the row and the rating rode on it. The invariant now reads *saved **or** rated*: un-saving keeps the row, the library still lists only saved problems, and clearing the rating on an un-saved one removes the last reason it exists. Nobody un-ticking a checkbox is asking to erase a note |
| "the rating isn't in my synced sheet" | Correct — the sheet was made before the column existed, and the app refuses to write a column it can't find by name (that's what stops it landing on your notes). Sync now adds it |
| "`done` comes up twice in the sheet" | It did: the site owns `done`, and `status` was also a *suggested* column for writing the same thing by hand. Dropped from the suggestions, and sync now keeps one canonical layout — the site's columns first, `solution_summary` leading the ones you write, yours after that. The reshape uses column *moves*, so formulas and formatting survive it, and the only column ever deleted is a duplicate with nothing in it. An existing `status` column is kept and still shown, just not suggested |
| "we can skip bookmarked / done / concept / tactics / time_taken / notes as defaults" | The sheet is now six app columns plus `solution_summary`, and that's it. Everything dropped was either state the site already shows (`done`, `bookmarked`) or a guess at how you'd take notes (`concept`, `tactics`, `time taken`) — and a guessed column is a column you have to look past. The app's *own* retired columns are removed on the next sync, because a `done` column frozen at last month's answer is worse than none; columns **you** wrote are never removed, only moved right |
| "sync sheet requires pressing sign in again every session, and again for every small change" | The token is deliberately never stored, so every reload started with none — and the rule "only ask on a click" then meant a click on every visit. Page loads now ask for one in the background, and any change queues a debounced write, so the sheet keeps up without being told to |
| "just refreshing the site opens Google sign-in now" | My fix for the above, shipped wrong. `prompt: ""` skips the *consent* screen but still shows the account chooser — silent only if you're signed into exactly one Google account, which I was and the reporter wasn't. Background requests now use `prompt: "none"`, which may not display anything at all and fails instead; and if one ever does take long enough to have involved a human, the app stops asking on that browser permanently |
| "add more atcoder, and these problems — make sure dedup is taken care of" | 33 added, 17 already there, 4 on judges this corpus doesn't carry, 2 names that resolve to nothing (sent back rather than guessed). Names resolve against the cached Codeforces problemset and kenkoooo's AtCoder index, and **dedupe runs on the URL, never the title** — `Chocolate` is three different Codeforces problems and `Slimes` is four AtCoder ones. The labels needed a second pass: gpt-4.1 writes `stars-and-bars` and `euler-totient`, which nobody searches for until they already know the answer, so specific labels now imply their family (`combinatorics`, `number-theory`). No benchmark regression on any slice ([experiments/12](experiments/12-math-and-dsu-growth.md)) |
| "sync sheet is not working — 400 in the console" | Two bugs behind one symptom. The 400 was `/api/track`: nine event types the client sends (every filter chip, the difficulty controls, "my level", the rating) were never added to the server's allowlist, so they were rejected on arrival — invisible in the app, because a beacon nobody awaits can't report a failure, and quietly missing from the stats page for weeks. Fixed, with a test that now reads both lists and fails the build if they drift apart. Separately, the sheet's new column tidy-up could fail the whole sync; it's non-fatal now (the sync writes your rows either way, since every write finds its columns by name) and every Google call names itself in the error, so the next report says *which* call broke |
| "let me add notes when I mark something, without clicking sync every time" | `✎` on the card you just marked opens a note editor in a popup — a plain box with markdown that survives a spreadsheet cell (`**bold**`, `` `code` ``, fenced code blocks with a language, bullets), ctrl+b / ctrl+e / ctrl+enter, and Tab that indents instead of leaving the box, since that is where code goes. It writes `solution_summary`, the one column of yours the app touches, and only the cell you typed into — markdown that survives a spreadsheet cell. Saving is **local-first**: the note is in your browser the instant you press save and shows on the card immediately, and the sync that already runs a few seconds after any change carries it into the sheet. No token, no sheet, no network? It waits, and the sync button carries a count of what's waiting. `:all` also moved to the front of the library bar, since that's the list you open to write |
| "2252F never got a `centroid` label — do tree labels like centroid and diameter even work?" | Two of them didn't. `tree-centroid` existed on two problems but was in nobody's vocabulary — not canonical, not an alias — and the bare words people type, `centroid` and `diameter`, reached nothing at all. Both are aliases now and `tree-centroid` is canonical (it is not `centroid-decomposition`: one is a vertex, the other a divide-and-conquer scheme, and aliasing them would be wrong). The `tree algorithms` umbrella was also listing an *alias* as a member, so it expanded to a label no problem carries; fixed and widened from 9 to 16, covering 184 problems |
| "in dense, rat and mouse should be allowed to match" | They should — the embedding ranker had a good answer for `rat` (*Cat and Mouse*, *Save More Mice*) and was never asked, because the unknown-word guard ran before the ranker and applied to both equally. It is ranker-aware now: keyword still refuses, meaning answers and labels the answer as nearest-by-meaning. The line between a word and a keyboard mash is **shape**, not score — `xkcdqq` scores 0.565 to `rat`'s 0.296, so no cutoff works, while a vowel check plus "no tripled letter, no five-consonant run" separates them cleanly. A name passes that test, so `deepya` gets labelled results now instead of zero |
| "codeforces problems are stuck waiting for statements" | They were: codeforces.com 403s a script, the official mirrors serve a JavaScript challenge, and the open-r1 dataset lags live contests by weeks — 27 problems from four August rounds were queued. Tavily's extract endpoint gets through (`extract_depth: "advanced"`; the basic depth stops before the statement). All 27 landed for about 12 of the month's 1000 free credits. The dataset path stays as the free fallback for anything old enough |
| "the harder problems need better labels than gpt-4.1 gives, without paying more" | Div-2 E/F/G problems now go to Claude subagents that read the statement and work out the solution, and their proposals merge through the review queue the repo already had. The cheap model still does A–D. The prompt also changed for everything: it used to ask for *the most discriminative* technique, which optimises for the setter's solution — it now asks for every technique that genuinely solves the problem, because someone searching "two pointers" should find a problem that binary search also solves |
| "\"rat\" becomes \"sat\" — is Levenshtein even the right tool here?" | The distance was right; the question was wrong. At three characters one edit is a third of the word and this corpus has hundreds of three-letter terms, so *something* is always one edit away — `rat`→`sat`, `bat`→`bit`, `seg`→`set`, and `graf`→`gray`, a word three problems happen to contain. Fixed with evidence rather than a new algorithm: the first letter must match (`prefix_length: 1`, what Elasticsearch does), a three-letter word can only be **completed** rather than respelled, and a short target needs real usage behind it. Every good correction survives — `djikstra`, `stak`, `hepa`, `arry`, `tre`→`tree` — and the benchmark is flat to four decimals. (Phonetic matching would have made this worse: `rat` and `sat` are phonetically adjacent by construction) |
| "\"graph\" and \"graphs\" give very different results" | Very different, and the plural was wrong: `graph` returned 546 problems, `graphs` returned 18 — led by *Kefa and Park* — because a rare token carries a huge IDF, so the few documents containing the plural outranked every actual graph problem. `trees` returned *Cut Off Trees for Golf Event*. Fixed with s-stemming **guarded by the corpus vocabulary**: a plural folds into its singular only when that singular is a word this corpus has, so `graphs`→`graph` and `vertices`→`vertex` while `series`, `bfs` and `kruskals` are untouched. Mean top-10 agreement between singular and plural went **8.9% → 100%** ([experiments/13](experiments/13-plural-folding.md)). The 71-query benchmark could never have caught this — every query in it used the same grammatical number as the corpus — so it gained a `plural` slice and a standing agreement check |
| "let me filter the library by what I've written notes for — and it'll bloat once things get auto-bookmarked" | Two things, because the second is the real one. `notes: written up / no note` filters client-side, since notes live in your sheet and the server has never seen one; `to write up` is the chip for done-and-never-explained, which is what the revision backlog actually is. But filters stop scaling — so **a library view now takes a query**: `:done graph`, `:bookmarks segment tree`. That needed the command parse to stop being an exact map lookup, which nine separate places were each doing by hand. Plus `pick one`, for when you want to revise and not choose |
| "make something fun for the 404 page — dsa-flavoured if possible" | There was no 404 page at all: a typo'd URL fell off the end of the router and got Express's `Cannot GET /prfoile.html`. Now a real 404 that leads with a search box, and under it **unbalanced** — openers drop onto a stack, you press the matching closer to pop, and it ends in a `stack overflow` or an expression that stopped being valid, printed back with a caret under the character that broke it. Valid Parentheses is one of the four anchor problems this corpus is benchmarked on. Writing a fake-DOM harness to play it headlessly caught two bugs on the spot: the keypress that *started* a run was also played against the empty stack it created, and `resume` silently began a new game |

## Measured

Every ranking decision traces to an 81-query labeled benchmark (P@k, MRR, nDCG@10, Recall@100) with keyword / paraphrase / technique / title / plural slices — it's why BM25 stays the default and the fancier rankers are opt-in. The numbers and their write-ups live in [experiments/](experiments/).

## Ideas

Things worth building, and the honest reason they aren't built yet.

- **One difficulty scale across all four judges.** The biggest missing piece, and the first thing I'd like an opinion on — the constraints and the measurements are written out in [What I'd like feedback on](#what-id-like-feedback-on). Short version: filtering and sorting shipped _per judge_, which sidesteps the hard part rather than solving it, and CSES's 400 problems (12.5% of the corpus) carry no difficulty at all.
- **Difficulty relative to you** — shipped as the `my level` chip, per judge ([how it works](#how-my-level-picks-a-band)). It covers the 1,350 rated Codeforces and AtCoder problems directly and LeetCode through a coarser proxy; CSES still has nothing to go on.
- **Multi-technique queries, and the practice set they'd unlock.** `fenwick graph` doesn't return problems that need both — BM25 scores one bag of words, so the rarer term wins and you get mostly Fenwick problems. Running each technique as its own query and drawing a stratified sample across them would fix that, and it's also the shape a "give me N problems for OA prep" feature needs. Both want the same missing piece — a difficulty scale that works across four judges — which is why that scale is the highest-value next thing rather than another judge.
- **Expansion scoped to the lexical rankers only.** Appending a technique's slug words helps BM25 and mildly hurts the embedding, which has no meaning for `sos` or `wqs` ([experiments/07](experiments/07-medium-hardest-growth.md)). The fix needs a seam that doesn't exist: the route expands once and hands the same string to both legs, `HybridIndex.search` accepts no options at all, and the dense index drops its fourth argument. Phrase consumption cut the pollution at source, which was the cheaper half of the win.
- **Phrase and proximity scoring.** Postings are `Set<docId>` with no positions, so bigrams or span scoring mean restructuring the index. The alias table gets the same outcome for the technique names people actually type, which is where every report has come from — worth revisiting only if *unknown* phrases start mattering.
- **Stemming beyond plurals** — `merge`/`merging`, `sort`/`sorting`. Plurals shipped ([experiments/13](experiments/13-plural-folding.md)) once a user reported that `graph` and `graphs` returned different things; the rest of Porter has not, because it also produces `matrices`→`matric` and `series`→`seri`, and in a vocabulary full of names a wrong stem is a silent recall bug. One thing I had wrong here for months: stemming does **not** invalidate the committed embeddings — the sentence encoder reads `problemText` raw, and only the lexical tokenizer folds.
- **The truly exotic techniques.** Segment tree beats, link-cut trees beyond the one problem that has one, wavelet trees, Blossom, push-relabel, Gomory-Hu, parallel binary search, CDQ divide-and-conquer. These are real and they do appear at 3000+, but this corpus has ~0 problems for each, and a label with no problems is worse than no label — it answers a search with silence and shows up empty on the patterns page. Adding them means sourcing problems first, not vocabulary first.
- **Recommendations from what you've solved.** Done-marks, judge ratings and now the per-problem recall rating (`again` / `hard` / `medium` / `easy`) are enough for a rule-based "next problem" heuristic before anything fancier is warranted — the missing piece is no longer the signal, it's whether a queue is better than the two filters that already compose into one.

**On feasibility:** latency isn't the obstacle for any of these. Sorting or banding an already-ranked result list costs well under a millisecond at this corpus size — the same place the judge filter does its work, after ranking and before paging. The obstacle is a common difficulty scale across four judges. The second obstacle turned out to be a product decision rather than an engineering one — ordering by anything other than relevance has to say what happens to the ranking, and to paging, before it can be built; per-judge sort ships because answering that for one judge is honest, and answering it for four isn't.

## What gets logged

Search counts, latency percentiles and a click-through funnel go to a Postgres `events` table and surface on [/stats.html](https://onebysec.com/stats.html). No IPs, no user agents — an anonymous cookie id, plus your user id if you're signed in.

Two things learned the hard way and now fixed in code:

- **Only the deployed app writes events** (`NODE_ENV=production`). Local development points `DATABASE_URL` at the same database, so without that gate every dev restart and test query lands in the public stats — which is exactly what happened: two thirds of recorded "visitors" turned out to be a curl loop, and the top-queries list was a load test. Numbers you publish have to come from real usage or they're not numbers.
- **The search counter counts keystrokes, not questions.** Typing is debounced, so `graph` logs `g`, `gr`, `gra`… Measured at 38% of search events being a prefix of the next one typed within 5 seconds. Worth knowing before reading any total on that page.

### Sheet sync, if you self-host

The notes feature needs a (free) Google OAuth client: create a Google Cloud project → **enable both the Google Sheets API and the Google Drive API** (Drive is used to create and re-find the one sheet; a missing Drive API is the usual cause of a 403 on first sync) → OAuth consent screen (External, scope `drive.file`) → Web application client ID with your origins (e.g. `http://localhost:3000`) under *Authorized JavaScript origins* → set `GOOGLE_CLIENT_ID` in the environment. The feature hides itself when the variable is unset. No client secret is involved anywhere — the token flow is entirely in-browser. While the OAuth app is in *Testing* status only listed test users can connect; **publish it** to lift that — with only the non-sensitive `drive.file` scope there is no verification review, no user cap, and no cost. The "100 user cap" shown in the console applies to unapproved *sensitive* scopes, which this app never requests.

## Keeping the corpus fresh

New problems come from contest links:

```bash
python3 scripts/ingest_contest.py https://leetcode.com/contest/weekly-contest-513/
python3 scripts/ingest_contest.py --retry-pending     # drain the Codeforces backlog
```

It stages, prints the next commands (annotate → embed → validate), and never commits — you read the diff, then `git push`, and the deploy rebuilds.

Codeforces statements come from [Tavily](https://tavily.com)'s extract endpoint (`scripts/fetch_statements.py`), because codeforces.com 403s a script and the mirrors serve a JavaScript challenge. Use the `/contest/<id>/problem/<index>` URL, not `/problemset/...` — the extractor drops `<ul>` content on the latter for some problems, which silently removed the *operation* from a problem whose whole point was the operation. Ratings arrive weeks after a contest, so `scripts/refresh_cf_ratings.py` goes back for them.

A list of problem *names* works too — write the URLs into a `Problems/urls_*.txt` file and run `annotate_problem_urls.py --urls <file>`. Names resolve against the cached Codeforces problemset and kenkoooo's AtCoder task list; dedupe runs on the **URL**, never the title, because `Chocolate` is three different Codeforces problems. Most recent batch: [experiments/12](experiments/12-math-and-dsu-growth.md), 33 number-theory / combinatorics / DSU problems, no benchmark regression.

Two things worth knowing:

- **LeetCode Q1 is skipped.** A contest is four problems worth 3/4/5/6 points; Q1 is the warm-up and is nearly always Easy, and this corpus has no Easy filler. The rule keys on `credit` rather than the difficulty label, because credit is structural and available the moment the contest ends while the label can still move.
- **Codeforces problems wait in a queue.** codeforces.com is Cloudflare-blocked to scripts, so statements come from the [open-r1/codeforces](https://huggingface.co/datasets/open-r1/codeforces) dataset — which lags live contests, and fresh contests report `rating: null` for weeks besides. Rather than admit a problem with no statement (nearly unsearchable, and it would dilute the benchmark), the ingest records it in `data/pending_contests.json` and `--retry-pending` picks it up the week its statement lands.

**Hard problems get their reasoning kept.** When an agent labels a Div-2 E/F/G problem it derives the solution first and usually verifies it against a brute force — and until now only three or four labels survived that. [data/analysis/](data/analysis/) keeps the derivation itself, one file per problem, so the next pass over the vocabulary starts from what was already worked out instead of re-deriving it. It is deliberately **not** searchable: the indexed text is title + statement + tags + patterns, and nobody wants a solution in the search results for the problem it solves.

**Ingest is a build-time activity, deliberately.** There's no admin page and no OpenAI key on the server: the corpus is baked into the Docker image and loaded once at boot, Render's filesystem is ephemeral (anything written at runtime dies on the next restart), and the container ships without the ingest scripts or a Python runtime at all. That constraint is what makes `corpusHash` a real guarantee rather than a hope — the embeddings can never drift from the text they were built from, because both are fixed at build time.

## Run it

```sh
cp .env.example .env   # fill DATABASE_URL (Neon or local docker) + JWT_SECRET
npm install
npm run db:migrate
npm run dev            # http://localhost:3000
```

```sh
npm run test:search    # rankers, routes, and the browser bundles
npm run validate       # corpus schema, bench ids, embedding freshness
npm run bench          # the 71-query benchmark, per slice
```

Eighteen test files. Fourteen run in `test:search` with no database at all; `npm run test:profile` adds the fetchers and the handle cipher, and the last two (auth, user-state) are round-trips that truncate tables and want a dev database pointed at them. The ones worth knowing about are the odd ones: `web/app.smoke.test.js` is a static lint that catches a browser bundle calling a function it no longer declares (that shipped twice) and now also checks every `track()` beacon against the server's allowlist; `web/sheets.test.js` replays the column-reshape plan through a simulator to prove no cell of yours is ever lost; `web/help.test.js` fails any manual line over 64 columns.

Corpus embeddings ship in the repo; the first boot downloads the ~25 MB embedding model once into `.model-cache/`. No database? The app still boots and searches — only accounts/bookmarks need Postgres (`npm run services:start` brings up a local one on :5433).

## Author

Written and maintained by **Dedeepya Avancha** — [github.com/Dd1235](https://github.com/Dd1235) · [onebysec.com](https://onebysec.com). The design decisions, the benchmark and the write-ups in [experiments/](experiments/) are all mine; each one is traceable through the commit history of [Dd1235/Cosine](https://github.com/Dd1235/Cosine).

No licence is granted: the source is public to read and to check my claims against, not to relabel. If you'd like to use a piece of it, or write about it, ask — the answer is usually yes.

© 2026 Dedeepya Avancha.
